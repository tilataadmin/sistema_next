# Bitácora — Módulo de Planeación: Extensión Programa del Diploma (PD)

**Proyecto:** SchoolNet — Colegio Tilatá  
**Rama activa:** `developmen` → `main`  
**Entornos:** DEV `spjzvpcsgbewxupjvmfm` · PROD `mrtuerkncqodhakuwjob`  
**Última actualización:** 4 de junio de 2026

---

## 1. Resumen del alcance

La extensión PD agrega al módulo de planeación (`pln_`) dos nuevos tipos de documento:

| Documento | Tabla principal | Identidad única | Naturaleza |
|---|---|---|---|
| Esquema bi-anual (Esquema del curso) | `pln_dp_outlines` | `(subject_id, level)` | Vivo — sin año académico |
| Planeador trimestral PD | `pln_planners` + `pln_planner_dp` | `(subject_id, grade_id, trimester, academic_year_id, level)` | Por año académico |

El esquema bi-anual cubre los dos años del programa (Año 1 = 10°, Año 2 = 11°) para una asignatura y nivel (SL/HL). El planeador trimestral es el documento operativo de cada trimestre.

---

## 2. Decisiones de diseño registradas (D1–D13)

| ID | Decisión |
|---|---|
| D3 | SL/HL es discriminador de identidad — genera planeadores separados |
| D4 | El esquema bi-anual es un documento vivo: una sola versión por `(subject_id, level)` |
| D6 | Cronograma en planeador PD = ciclos → sesiones (`pln_planner_sessions`) |
| D7 | PD no usa rúbrica 2–5; la evaluación es descriptiva (formativa/sumativa) |
| D8 | Componentes sumativos con peso en `pln_planner_dp_components` |
| D10 | Visibilidad de reflexión a familias: boolean en `programs.share_reflection_with_families` |
| D11 | Esquema del curso = tabla estructurada en `pln_dp_outline_topics` (6 celdas: año 1 y 2 × trimestre 1–3) |
| D13 | Formularios PD en archivos dedicados (`dp-outline-form.html`, `dp-planner-form.html`) |
| D14 | "Plan lector" es un campo específico de asignaturas de Lengua/Literatura — parametrizado con `academic_subjects.has_plan_lector boolean` desde Configuración → Áreas y Asignaturas |

---

## 3. Estado de implementación

### Bloque 1 — Base de datos ✅ COMPLETO (DEV + PROD)

**5 tablas nuevas creadas, RLS deshabilitado:**
- `pln_dp_outlines` — encabezado del esquema bi-anual
- `pln_dp_outline_topics` — 6 celdas del Esquema del curso (UNIQUE `outline_id + program_year + trimester`)
- `pln_planner_dp` — cuerpo extendido 1:1 del planeador PD
- `pln_planner_sessions` — sesiones del cronograma por ciclo (UNIQUE `cycle_id + day_number`)
- `pln_planner_dp_components` — componentes sumativos con peso

**Alteraciones a tablas existentes:**
- `pln_planners`: columnas `level` y `dp_outline_id`; UNIQUE reconstruido como `uq_pln_planners_identity`
- `programs`: columna `share_reflection_with_families boolean DEFAULT false`
- `pln_comments`: CHECK ampliado para incluir `entity_type = 'dp_outline'`
- `academic_subjects`: columna `has_plan_lector boolean NOT NULL DEFAULT false`
- `pln_dp_outlines`: columna `chosen_works` renombrada a `plan_lector`

**Auditoría:**
- `audit_trigger_function()` actualizada con 5 bloques ELSIF nuevos
- 5 triggers de auditoría creados (total `pln_*`: 25 triggers)
- Función atómica `pln_create_planner_session(p_cycle_id)` creada

**Verificación PROD (confirmada):**
```
5 tablas nuevas: RLS = false ✓
uq_pln_planners_identity activo ✓
25 triggers pln_* ✓
share_reflection_with_families en programs ✓
CHECK pln_comments incluye dp_outline ✓
has_plan_lector en academic_subjects ✓
plan_lector en pln_dp_outlines ✓
```

---

### Bloque 2 — Permisos y sidebar ✅ COMPLETO (DEV + PROD)

**3 permisos nuevos en tabla `permissions`:**

| `permission_name` | `url_path` |
|---|---|
| Crear planeador bianual PD | `/modules/planning/my-dp-outlines.html` |
| Gestionar planeadores bianuales PD | `/modules/planning/dp-outlines.html` |
| Crear planeador trimestral PD | `/modules/planning/my-dp-planners.html` |

**Total permisos de planeación: 10** ✓

**`sidebar.js`** — PR `developmen` → `main` aplicado. Ítems PD visibles en el menú lateral de PROD ✓

> **Pendiente operativo:** asignación de los 3 permisos nuevos a los roles correspondientes en PROD (requiere terceros).

---

### Bloque 3 — UI del esquema bi-anual ✅ COMPLETO (archivos entregados)

#### Módulo de configuración — Áreas y Asignaturas ✅

Archivo modificado: `modules/configuracion/areas-subjects.html` (o equivalente en el repositorio).

**6 cambios aplicados:**
1. Query de `cargarAsignaturas()` incluye `has_plan_lector`
2. Badge visual "Plan lector" en la tabla si `has_plan_lector = true`
3. Checkbox "Tiene plan lector" agregado al modal de asignatura
4. `abrirModalAsignatura()` resetea el checkbox al abrir
5. `editarAsignatura()` carga el estado del checkbox desde BD
6. `guardarAsignatura()` incluye `has_plan_lector` en el payload de PATCH/POST

#### `my-dp-outlines.html` ✅

Ruta: `/modules/planning/my-dp-outlines.html`  
Estado: **archivo completo entregado** — pendiente commit a `developmen`.

Funcionalidades:
- Acceso con `validatePageAccessAny(['Crear planeador bianual PD', 'Gestionar planeadores bianuales PD'])`
- Carga de asignaturas del docente desde `academic_assignments` (sin filtro de año)
- Formulario: asignatura + nivel (SL/HL)
- Validación de existencia antes de crear (local + BD)
- Al crear: POST a `pln_dp_outlines` + 6 filas en `pln_dp_outline_topics` (Promise.all)
- Redirección a `dp-outline-form.html?outline_id=...`
- Tabla: asignatura, nivel (badge SL/HL), completado por, estado, última edición
- Click en fila → abre formulario

#### `dp-outline-form.html` ✅

Ruta: `/modules/planning/dp-outline-form.html`  
Estado: **archivo completo entregado** (1.136 líneas) — pendiente commit a `developmen`.

Funcionalidades:
- Acceso con `validatePageAccessAny(['Crear planeador bianual PD', 'Gestionar planeadores bianuales PD'])`
- Control de acceso (4 caminos): assignment activa, coordinador de área, director de programa, director de sección
- Modo solo lectura automático si ningún camino se cumple
- Header informativo: badge de asignatura, badge de nivel (SL/HL), completado por, última edición
- Polling de edición concurrente cada 15 s sobre `pln_dp_outlines.updated_at`
- **Bloque 1 — Identificación:** select de workers, fecha de completado, período IB, nombre del taller, código del colegio. Autosave inmediato en select/fecha; debounce en textos.
- **Bloque 2 — Plan lector:** Quill. Oculto por defecto; visible solo si `academic_subjects.has_plan_lector = true`. Campo: `plan_lector`.
- **Bloque 3 — Esquema del curso:** 6 tarjetas colapsables organizadas por Año 1 (10°) y Año 2 (11°), cada una con 3 trimestres. Campos por celda: tema, contenidos, tiempo asignado, instrumentos de evaluación, recursos. Autosave debounce + flush en blur. Preview del tema en el header de cada tarjeta.
- **Bloques 4–9 — Narrativos (Quill):** evaluación IB, TdC, ATL, mentalidad internacional, perfil IB, recursos. Autosave debounce con filtro `source === 'user'`.
- **Bloque 10 — Comentarios:** `entity_type = 'dp_outline'`, respuestas anidadas, soft-delete, compositor de respuesta inline.
- `patchOutline(payload)` centralizado — actualiza `updated_at` y `lastKnownUpdatedAt`.
- `patchTopic(topicId, field, value)` — PATCH individual a `pln_dp_outline_topics`.

---

### Bloque 4 — UI del planeador trimestral PD ⏳ PENDIENTE

- [ ] `my-dp-planners.html` — listado y creación de planeadores trimestrales PD
- [ ] `dp-planner-form.html` — formulario completo PD trimestral:
  - Cuerpo `pln_planner_dp`
  - Componentes sumativos con peso (`pln_planner_dp_components`)
  - Cronograma: ciclos → sesiones (función atómica `pln_create_planner_session`)
  - Reflexión (3 campos)
  - Comentarios (`entity_type = 'planner'`)

### Bloque 5 — Vistas de coordinación ⏳ PENDIENTE

- [ ] Ramificación en `planners.html`, `coordinator-area.html`, `coordinator-program.html`: click en planeador con `level IS NOT NULL` → redirige a `dp-planner-form.html`
- [ ] Toggle de visibilidad de reflexión a familias (`programs.share_reflection_with_families`)

### Bloque 6 — PR a main ⏳ PENDIENTE

- [ ] Commit de `my-dp-outlines.html` a `developmen`
- [ ] Commit de `dp-outline-form.html` a `developmen`
- [ ] PR `developmen` → `main` para desplegar todos los archivos nuevos

### Bloque 7 — Manuales ⏳ PENDIENTE

- [ ] Manuales HTML para las pantallas nuevas (según `GUIA_ELABORACION_MANUALES_SCHOOLNET_v4_0`)

---

## 4. Pendientes operativos (requieren terceros)

- [ ] Asignar los 3 permisos PD a los roles correspondientes en PROD
- [ ] Poblar `grades.program_id` para 10° y 11° en PROD
- [ ] Poblar `academic_areas.coordinator_worker_id` para las áreas PD en PROD
- [ ] Verificar `programs.program_director_email` del programa PD en PROD

---

## 5. Deuda técnica relevante

- Validación atómica de `cycle_number` y `objective_number` pendiente antes de uso masivo en producción
- Polling de concurrencia pendiente de aplicar retroactivamente a `unit-form.html`
- RLS audit pendiente en ~10 tablas (varias `ai_*` usan service key intencionalmente desde `tilata-ia`)
- Migración de API key Supabase pendiente para PresuCorti

---

## 6. Arquitectura de rutas — módulo PD

```
Menú lateral
├── Crear planeador bianual PD → my-dp-outlines.html
│       └── [crear / click fila] → dp-outline-form.html?outline_id=...
├── Gestionar planeadores bianuales PD → dp-outlines.html  [pendiente]
└── Crear planeador trimestral PD → my-dp-planners.html    [pendiente]
        └── [crear / click fila] → dp-planner-form.html?planner_id=...
```

---

*Documento generado por Desarrollos / Claude — SchoolNet v26.6.4*
